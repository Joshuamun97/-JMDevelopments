<!-- Loop over each object in the projects array -->
<div class="mb">
    <h1 class="d-flex justify-content-center pb-5 text-white text-monospace">Projects</h1>
</div>
<div class="container">
    <div class="row d-flex justify-content-center">
        {{#each projects as |project| }}
        <div class="col-sm-4 card-deck card-specs hvr-grow-shadow m-4 text-white text-monospace d-flex justify-content-center" style="width: 18rem;">
            <img class="card-img-top" src="..." alt="Card image cap">
            {{!-- <h1>{{project.name}}</h1> --}}
            {{!-- <div class="card-body">This is the cards body</div> --}}
            <h2 class="card-title">{{project.name}}</h2>
            <p class="card-text">{{project.description}}.</p>
             <a href="#" class="btn btn-primary mb-3">Go somewhere</a>
        </div>

{{/each}}
</div>
{{!-- <div class="card" style="width: 18rem;">
    {{#each projects as |project| }}
  <img class="card-img-top" src="..." alt="Card image cap">
  <div class="card-body">
    <h5 class="card-title">{{project.name}}</h5>
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
    <a href="#" class="btn btn-primary">Go somewhere</a>
  </div>
  {{/each}} --}}
</div>

</div>

{{!-- <div class="container project">
    <div class="col-sm">
        <h2>
            <!-- Call helper function with triple brackets to render the emoji span as HTML -->
            <a class="text-white text-monospace" href="/project/{{project.id}}">{{project.name}}</a>
        </h2>
        <p>
            {{!-- <span for="img" aria-label="money">💲</span>
            <!-- Pass needed_funding value to the helper function -->
            <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
            needed! --}}
            {{!--
        </p>
        <p>Created by {{project.user.name}} on {{format_date project.date_created}}</p>
    </div>
    <div class="col-md-7">
        <p>
            {{project.description}}
        </p>
    </div>
</div> --}}